# oauth2client is deprecated upstream and ships no type information; no stubs
# package exists (types-oauth2client is not on PyPI). Ignoring ONLY its
# missing types -- everything mypy can check in our code is still checked.
[mypy]

[mypy-oauth2client.*]
ignore_missing_imports = True
