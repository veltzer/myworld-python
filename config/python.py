config_requires = []
dev_requires = []
install_requires = [
    "google-api-python-client",
    "click",
]
make_requires = [
    "pymakehelper",
    "pydmt",
    "pyclassifiers",
]
test_requires = []
requires = config_requires + install_requires + make_requires + test_requires
